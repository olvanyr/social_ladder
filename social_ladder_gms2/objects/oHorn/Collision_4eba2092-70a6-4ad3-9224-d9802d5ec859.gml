
if state == "death" || state == "roll" || state == "dead" || state = "speak" || state = "stun" || state = "transform" || form = "invisible" exit;

create_hitbox(x, y, self, mask, 3, 2, contact_damage, image_xscale);
key = global.key;

text = "key";

c_background = c_gray;


x_buffer = 10;
y_buffer = x_buffer;

s_height = sprite_get_height(sKey_door1) + (2*x_buffer);
s_width = sprite_get_width(sKey_door1) + (2*y_buffer);
gui_height = display_get_gui_height();
gui_width = display_get_gui_width();

